steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - gcloud
  - functions
  - deploy
  - funcion_gcp
  - --runtime=python39
  - --region=europe-west1
  - --trigger-type=git-push
  - --allow-unauthenticated
  - --source=/mi_app